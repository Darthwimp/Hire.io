import 'package:flutter/material.dart';

class hiringScreen extends StatelessWidget {
  const hiringScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("hiring Screen"),
    );
  }
}
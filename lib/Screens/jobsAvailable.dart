import 'package:flutter/material.dart';

class jobsAvail extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("WORKING!!!!!!!"),
    );
  }
}